// Schema Prisma do Jera Horizonte.
// Entidades detalhadas serão criadas na Sessão 1.

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider  = "postgresql"
  url       = env("DATABASE_URL")
  directUrl = env("DIRECT_URL")
}

